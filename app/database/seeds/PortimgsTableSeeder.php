<?php

class PortimgsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('portimgs')->truncate();

		$portimgs = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('portimgs')->insert($portimgs);
	}

}
